region:
  name: Mumbai
  roi: [72.77, 19.00, 73.00, 19.30]

dates:
  # Use \"auto\" to follow rolling 6y/1y windows; override with explicit dates only for offline experiments.
  past: ["auto", "auto"]
  recent: ["auto", "auto"]

satellite:
  dataset: COPERNICUS/S2_SR_HARMONIZED
  cloud_threshold: 80
  scale: 10

detection:
  ndwi_threshold: 0.15
  pixel_resolution: 10
